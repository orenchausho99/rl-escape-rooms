# מדריך הגנה לפרויקט RL Escape Rooms

## 1. הסבר הפרויקט במשפט אחד

הפרויקט הוא קמפיין של חמישה חדרי בריחה, שבו סוכן לומד לפתור בכל חדר משימה אחרת באמצעות אלגוריתם אחר של למידת חיזוקים. ככל שהפתרון קצר ובטוח יותר, סכום התגמולים של הסוכן גבוה יותר.

## 2. מהי למידת חיזוקים

למידת חיזוקים מתארת אינטראקציה מחזורית:

1. הסוכן מקבל מצב `S` מהסביבה.
2. הוא בוחר פעולה `A`.
3. הסביבה עוברת למצב הבא `S'` ומחזירה תגמול `R`.
4. הסוכן מעדכן את הידע שלו.
5. התהליך נמשך עד למצב סופי או עד למספר הצעדים המרבי.

המטרה היא למקסם את ההחזר המצטבר:

```text
G_t = R_(t+1) + gamma*R_(t+2) + gamma^2*R_(t+3) + ...
```

`gamma` הוא מקדם ההיוון. ערך קרוב ל-1 נותן חשיבות גבוהה לתגמולים עתידיים.

מושגים מרכזיים:

- `State`: המידע שהסוכן צריך כדי לקבל החלטה.
- `Action`: פעולה שהסוכן יכול לבצע.
- `Reward`: משוב מספרי מיידי.
- `Policy`: כלל בחירת הפעולות של הסוכן.
- `Episode`: ניסיון מלא מתחילת החדר ועד הצלחה או מגבלת צעדים.
- `Terminal state`: המצב הסופי היחיד שמסיים את החדר.
- `Q(s,a)`: הערכת ההחזר הצפוי מבחירת פעולה `a` במצב `s`.

## 3. מבנה הקוד

הקוד מחולק לפי אחריות:

- `escape_room/envs.py`: המצבים, הפעולות, המעברים, המכשולים והתגמולים.
- `escape_room/algorithms.py`: Value Iteration, SARSA, Q-Learning ו-Approximate Q-Learning.
- `escape_room/replay.py`: סינון, מיון והצגת ספריית האפיזודות.
- `app.py`: ממשק Streamlit, משחקי Canvas, אימון, Replay, גרפים ושמירת תוצאות.
- `tests/test_requirements.py`: בדיקות אוטומטיות לדרישות הפרויקט.
- `static/game_art/`: התמונות והרקעים של המשחקים.

זרימת התוכנית:

```text
main()
  -> Welcome: הזנת שם שחקן או כניסה כאורח
  -> Campaign Dashboard
  -> בחירת חדר
  -> יצירת הסביבה המתאימה באמצעות make_env()
  -> Play / Train / Replay / Analytics / Room Specs
  -> run_training()
  -> האלגוריתם המתאים
  -> שמירת metrics וכל האפיזודות
```

הגרפיקה אינה הסביבה שממנה הסוכן לומד. המשחק בדפדפן מציג את אותם חוקים, אבל האימון מתבצע מול מחלקות Python דרך `reset()` ו-`step()`.

## 4. הממשק

הממשק בנוי בשלוש רמות ברורות:

1. `Welcome / Player Setup`: מזינים שם תצוגה או בוחרים `Continue as guest`.
2. `Campaign Dashboard`: רואים את מספר החדרים שהושלמו, מספר ריצות האימון, שיעור ההצלחה הטוב ביותר וכפתור להמשך מהחדר האחרון.
3. `Room Workspace`: נכנסים לחדר מסוים ועוברים בין משחק, אימון, Replay, גרפים ומפרט הסביבה.

שם השחקן הוא פרופיל מקומי של סשן Streamlit בלבד. אין כאן חשבון משתמש, סיסמה או מערכת הרשאות, והשם אינו משנה את הסביבה, התגמולים, הפרמטרים או האלגוריתמים. אפשר להיכנס כאורח כדי להתחיל מיד בהדגמה.

לכל חדר יש חמישה אזורים:

- `Play Game`: משחק ידני עם המקלדת כדי להבין את המשימה.
- `Train Agent`: שליטה בפרמטרים ואימון הסוכן.
- `Episode Replay`: צפייה בכל ניסיון שנשמר באימון.
- `Analytics`: גרפי התקדמות הלמידה.
- `Room Specs`: הצגת המצב, הפעולות, המודל והתגמולים.

`st.session_state` שומר את שם השחקן, החדר הנבחר, החדר האחרון, הריצה האחרונה, ריצות לפי חדר והחדרים שהושלמו. הנתונים נשמרים בזמן הסשן ומאפשרים ל-Dashboard להציג התקדמות ולהחזיר את המשתמש לחדר האחרון. חדר נחשב פתור כאשר שיעור ההצלחה ב-50 האפיזודות האחרונות הוא לפחות 60%. בחדר הראשון נבדקים כל 12 ה-rollouts של המדיניות.

## 5. חדר 1: Pac-Man Ice Maze

### המשימה

להגיע מ-`(0,0)` ליציאה `(9,9)`, להימנע מרוחות וממלכודות ולהתמודד עם תאים חלקלקים.

### הגדרת ה-MDP

- גודל: `10x10`.
- מצב: `(row, col, collected_mask, ghost_phase)`.
- פעולות: למעלה, ימינה, למטה ושמאלה.
- מצב סופי: הגעה ל-EXIT.
- מודל: ידוע במלואו.

`ghost_phase` הוא חלק מהמצב כדי לשמור על תכונת מרקוב. אם מיקום הרוחות לא היה במצב, אותו מיקום של השחקן היה עלול להוביל לתוצאה שונה בלי שהסוכן ידע מדוע.

בתא חלקלק, הפעולה מתבצעת כמתוכנן בהסתברות `1-p`, פונה שמאלה בהסתברות `p/2`, או ימינה בהסתברות `p/2`.

### תגמולים

- כל צעד: `-1`.
- יציאה: `+110`.
- מלכודות: `-22` או `-25`.
- פגיעה ברוח: `-45` וחזרה להתחלה.

העונש לכל צעד גורם למסלול קצר לקבל החזר גבוה יותר.

### האלגוריתם: Value Iteration

מכיוון שהמודל ידוע, אפשר לחשב את התוחלת של כל מעבר:

```text
V(s) = max_a sum_s' P(s'|s,a) * [R(s,a,s') + gamma*V(s')]
```

הקוד עובר שוב ושוב על כל המצבים. בכל מעבר הוא מחשב את ערך כל פעולה ושומר את המקסימום. האימון נעצר כאשר השינוי המרבי `delta` קטן מ-`theta`, או כאשר מגיעים למספר האיטרציות המרבי.

לאחר ההתכנסות נוצרת מדיניות שבוחרת בכל מצב את הפעולה בעלת הערך הגבוה ביותר. כאן אין אפיזודות למידה רגילות, ולכן ה-Replay שומר 12 הרצות מקריות של המדיניות שנמצאה.

פרמטרים מומלצים: `gamma=0.96`, `theta=0.0001`, `slip=0.25`, עד `1000` sweeps.

## 6. חדר 2: Sokoban Vault

### המשימה

לדחוף ארגז מ-`(0,8)` אל המטרה `(0,9)`, ואז להגיע לכספת שב-`(9,9)`.

### הגדרת הסביבה

- גודל: `10x10`.
- מצב: `(player_row, player_col, box_row, box_col)`.
- פעולות: ארבעת הכיוונים.
- מצב סופי: הארגז על TARGET והשחקן ב-SAFE.
- מודל: אינו ידוע לסוכן.
- קיימים תאים חלקלקים.

אם השחקן נכנס לתא הארגז, הקוד בודק אם התא שאחריו פנוי. רק במקרה כזה הארגז נדחף. לכן זהו Sokoban אמיתי ולא פריט גרפי בלבד.

### תגמולים

- כל צעד: `-1`.
- ארגז מגיע למטרה: `+28`.
- ארגז עוזב את המטרה: `-28`.
- סיום בחדר SAFE: `+130`.
- תנועה חסומה או דחיפה לא חוקית: `-6` נוסף.
- לייזרים: `-30` או `-35`.

### האלגוריתם: SARSA

SARSA הוא אלגוריתם `on-policy`. העדכון משתמש בפעולה הבאה שהמדיניות החוקרת באמת בחרה:

```text
Q(S,A) <- Q(S,A) + alpha * [R + gamma*Q(S',A') - Q(S,A)]
```

השם SARSA מגיע מהרצף `State, Action, Reward, State, Action`.

הפעולות נבחרות באמצעות `epsilon-greedy`: בהסתברות `epsilon` נבחרת פעולה אקראית, ובשאר הזמן נבחרת הפעולה עם ערך Q הגבוה ביותר. לאחר כל אפיזודה epsilon קטן, עד `epsilon_min`.

היתרון של SARSA בחדר מסוכן הוא שהוא לומד את הערך של ההתנהגות החוקרת בפועל, ולכן עשוי להעדיף מסלולים בטוחים יותר.

## 7. חדר 3: Bomberman Reactor

### המשימה

לאסוף שני COREs, להימנע מפצצות ומסיירים, להשתמש ב-WARP לפי הצורך, ואז להגיע ל-GATE.

### הגדרת הסביבה

- גודל: `10x10`.
- מצב: `(row, col, core_mask, guard_phase)`.
- `core_mask` הוא מסכת ביטים שמתארת אילו משני הפריטים נאספו.
- `guard_phase` מתאר את שלב התנועה של המסיירים.
- מצב סופי: שני ה-COREs נאספו והשחקן הגיע ל-GATE.
- מודל: אינו ידוע לסוכן.

### תגמולים

- כל צעד: `-1.2`.
- כל CORE בפעם הראשונה: `+30`.
- סיום: `+170`.
- פצצות: בין `-26` ל-`-38`.
- פגיעה במסייר: `-60` וחזרה להתחלה.
- WARP: תגמול `0`, כדי למנוע לולאת צבירת תגמול.

### האלגוריתם: Q-Learning

Q-Learning הוא `off-policy`. הפעולה בפועל עדיין נבחרת באמצעות epsilon-greedy, אבל יעד הלמידה מניח שבמצב הבא נבחר את הפעולה הטובה ביותר:

```text
Q(S,A) <- Q(S,A) + alpha * [R + gamma*max_a Q(S',a) - Q(S,A)]
```

ההבדל המרכזי מ-SARSA:

- SARSA משתמש ב-`Q(S',A')`, כלומר בפעולה שבאמת נבחרה.
- Q-Learning משתמש ב-`max Q(S',a)`, כלומר בפעולה החמדנית הטובה ביותר.

## 8. חדר 4: Lunar Lander Pad

### המשימה

להזיז נחתת בחדר רציף בגודל `10x10` מטרים, לעבור בין שדות אסטרואידים ולהגיע למשטח הנחיתה.

### מצב ופעולות

המצב הוא בדיוק:

```text
(X, Y, Vx, Vy)
```

המיקום רציף, אבל המהירות דיסקרטית. לכל אחד מ-`Vx,Vy` יש ערך מתוך `{-1,0,1}`, ולכן קיימות תשע פעולות. בכל צעד:

```text
X' = X + Vx * speed * 0.02
Y' = Y + Vy * speed * 0.02
```

הכניסה לרדיוס המטרה מחזירה מצב סופי קנוני: מיקום המטרה ומהירות אפס.

### תגמולים

- כל צעד זמן: `-0.015`.
- התקדמות: `5 * (מרחק קודם - מרחק חדש)`.
- הגעה ל-PAD: `+55`.
- פגיעה בקיר: `-0.6`.
- פגיעה באזור אסטרואידים: `-2.5`, ביטול התנועה ואיפוס המהירות.

התגמול על התקדמות נקרא `reward shaping`. הוא נותן לסוכן אות למידה גם לפני שהגיע ליעד.

### Approximate Q-Learning

אי אפשר לשמור טבלת Q לכל מיקום רציף אפשרי. לכן מקרבים את Q באמצעות תכונות ומשקולות:

```text
Q(s,a) = sum_i w_i(a) * f_i(s)
```

התכונות כוללות מיקום מנורמל, כיוון ומרחק למטרה, מהירות, ושש חלוקות מוזזות של המרחב הנקראות tile coding. לכל פעולה יש קבוצת משקולות משלה.

שגיאת ה-TD היא:

```text
delta = R + gamma*max_a Q(S',a) - Q(S,A)
```

המשקולות של התכונות הפעילות מתעדכנות בכיוון שמקטין את השגיאה.

## 9. חדר 5: Portal Hazard Run

זהו חדר הרשות והוא הקשה ביותר. הוא משתמש באותו Approximate Q-Learning, אבל מוסיף מכשולים דינמיים ותצפית חלקית.

המצב:

```text
(X, Y, Vx, Vy, obstacle_dx, obstacle_dy, visible)
```

המכשולים:

- רוחב `0.5` מטר.
- מספרם ניתן לשינוי.
- מיקומם וכיוון תנועתם נוצרים מחדש בכל `reset()`.
- כל מכשול נע אופקית או אנכית ומחליף כיוון בגבול.
- הסוכן רואה רק את המכשול הקרוב ביותר שנמצא לפניו ובטווח התצפית.

הקוד קובע שמכשול נמצא מלפנים באמצעות מכפלה סקלרית חיובית בין כיוון התנועה לווקטור אל המכשול.

פגיעה בפורטל נותנת `-8`, מעבירה את הסוכן למיקום בטוח ואקראי אחר ומאפסת מהירות. היעד האקראי נשאר רחוק מספיק מהיציאה, ולכן התנגשות אינה יכולה לשמש כקיצור דרך. בכך החדר שונה מחדר Pac-Man: הרוחות רודפות ומחזירות להתחלה, ואילו הפורטלים נעים במסלולים ואחראים לשינוי אקראי במיקום. אפשר לייצר seed חדש ב-Replay ולבדוק את המדיניות בחדר שלא הופיע באימון. זה בודק יכולת הכללה.

## 10. תפקיד ההייפר-פרמטרים

- `seed`: משחזר את אותה סדרת מספרים אקראיים לצורך ניסוי הוגן וחוזר.
- `episodes`: מספר ניסיונות האימון.
- `max_steps`: מגבלת צעדים באפיזודה.
- `alpha`: עוצמת העדכון. גבוה מדי עלול לגרום לחוסר יציבות, נמוך מדי ללמידה איטית.
- `gamma`: חשיבות העתיד.
- `epsilon`: הסתברות לחקירה אקראית.
- `epsilon_decay`: קצב הירידה בחקירה.
- `epsilon_min`: רמת החקירה המזערית.
- `slip_probability`: אי ודאות בתנועה על תא חלקלק.
- `theta`: תנאי עצירה של Value Iteration.
- `observation_range`: כמה מטרים קדימה חדר 5 יכול לראות.

## 11. אופטימיזציית הפרמטרים

הכפתור `Optimize and train` בודק ארבעה מועמדים. לכל מועמד מתבצע אימון קצר, ולאחר מכן מחושב ציון:

```text
score = success_rate*10000 + mean_reward - 0.05*mean_steps
```

הצלחה מקבלת עדיפות ברורה, אחריה תגמול, ואז פתרון קצר. המועמד הטוב ביותר משמש לאימון מלא והתוצאות נשמרות ב-`runs/tuning/`.

נקודה שכדאי להכיר: במימוש הנוכחי כל מועמד מקבל seed דטרמיניסטי אחר. זה בודק מועמדים על רצפים אקראיים שונים, אבל השוואה מחקרית חזקה יותר הייתה מריצה כל מועמד על אותה קבוצת seeds וממוצעת את התוצאות.

## 12. שמירת אפיזודות ו-Replay

בכל אפיזודה נשמרים:

- מספר האפיזודה.
- כל המצבים.
- כל הפעולות.
- התגמולים בכל צעד.
- התגמול הכולל.
- מספר הצעדים.
- הצלחה או כישלון.
- epsilon של אותה אפיזודה.
- בחדר 5 גם מיקום המכשולים בכל frame.

כדי לחסוך זיכרון, המצבים נשמרים במערכי NumPy מסוג `int16` או `float32`, והפעולות ב-`int8`. ה-Replay אינו סימולציה חדשה אלא שחזור של הנתונים המדויקים שהתרחשו באימון.

אפשר לסנן הצלחות וכישלונות, למיין לפי אפיזודה או תגמול, לבחור כל אפיזודה ולהפעיל, לעצור, להתקדם צעד אחד או לגרור את ציר הזמן.

## 13. Analytics ושמירת תוצאות

באלגוריתמי הלמידה מוצגים:

- Reward לכל אפיזודה.
- ממוצע נע על 25 אפיזודות.
- Steps לכל אפיזודה.
- שיעור הצלחה נע.
- epsilon לאורך האימון.
- שגיאת TD ממוצעת באלגוריתם המקורב.

בחדר 1 מוצגים `delta` וערך מצב ההתחלה כדי להראות התכנסות.

כל ריצה שומרת תיקייה תחת `runs/` ובה:

- `metrics.csv`
- `attempts.csv`
- `parameters.json`
- `summary.json`
- `learning_report.png`

## 14. שאלות צפויות בהגנה

### למה חדר 1 יכול להשתמש ב-Dynamic Programming?

כי פונקציית המעבר וההסתברויות ידועות. האלגוריתם יכול לחשב תוחלת בלי ללמוד רק מדגימות.

### למה SARSA נחשב on-policy?

כי יעד העדכון משתמש בפעולה הבאה שנבחרה על ידי אותה מדיניות epsilon-greedy שמייצרת את ההתנהגות.

### למה Q-Learning נחשב off-policy?

כי ההתנהגות יכולה להיות חוקרת, אבל יעד העדכון משתמש תמיד בערך הפעולה החמדנית הטובה ביותר.

### למה צריך epsilon?

ללא חקירה, הסוכן עלול להיתקע מוקדם במסלול שנראה טוב אך אינו אופטימלי.

### למה מקטינים epsilon?

בתחילת האימון צריך לחקור הרבה. בהמשך, לאחר שנצבר ידע, רוצים לנצל את המדיניות שנלמדה.

### למה המצב כולל mask ו-phase?

כי מיקום בלבד אינו מספיק. אותו תא יכול להיות שונה אם פריט כבר נאסף או אם אויב נמצא בשלב תנועה אחר.

### למה לא להשתמש בטבלת Q בחדר 4?

כי `X,Y` רציפים ויש מספר עצום, תאורטית אינסופי, של מצבים. קירוב מאפשר הכללה בין מצבים סמוכים.

### האם reward shaping משנה את הבעיה?

הוא מוסיף משוב ביניים שעוזר ללמוד. עדיין נשמר תגמול סופי ברור ועונש זמן, והמדיניות נבחנת לפי הצלחה ומהירות.

### מה ההבדל בין המשחק הידני לאימון?

המשחק הידני הוא שכבת Canvas אינטראקטיבית. האימון מתבצע בסביבת Python. בחדר 1 הרוחות במשחק הידני רודפות בזמן אמת, בעוד שב-MDP תנועתן מיוצגת במחזור דטרמיניסטי באמצעות `ghost_phase` כדי לשמור על מודל ידוע ושחזור מדויק.

### איך יודעים שהקוד עומד בדרישות?

קיימות בדיקות שבודקות שלושה חדרי `10x10`, תאים חלקלקים, מודל מעבר מלא, Sokoban אמיתי, מצב סופי יחיד, דינמיקה רציפה, `dt=0.02`, תשע מהירויות, מכשולים ברוחב `0.5`, תצפית מורחבת ושמירת כל האפיזודות.

## 15. תסריט מלא להגנה

התסריט מתאים להצגה של כ-10 עד 12 דקות. המשפטים כתובים בגוף ראשון כדי שיהיה אפשר לתרגל אותם כמעט מילה במילה.

### 15.1 פתיחה: כ-45 שניות

**מה להציג:** מסך ה-Welcome של האפליקציה.

**מה לומר:**

> שלום, הפרויקט שלי נקרא RL Escape Rooms. בניתי קמפיין של חמישה חדרי בריחה, כאשר בכל חדר יש משימה שונה ואלגוריתם אחר של למידת חיזוקים. רמת הקושי עולה מחדר לחדר. המטרה של הסוכן היא להגיע למצב הסופי בזמן קצר ובצורה בטוחה, ולכן בכל הסביבות קיים גם עונש על צעדים או על זמן. כך פתרון מהיר מקבל החזר מצטבר גבוה יותר.

### 15.2 כניסה ו-Campaign Dashboard: כ-45 שניות

**מה להציג:** להזין שם, ללחוץ `Start campaign` ולהציג את ה-Dashboard.

**מה לומר:**

> במסך הכניסה אפשר להזין שם שחקן או להיכנס כאורח. זהו רק שם מקומי לסשן ולא מנגנון הרשאות. לאחר הכניסה מגיעים ל-Campaign Dashboard. כאן אפשר לראות כמה חדרים הושלמו, כמה אימונים בוצעו ומה שיעור ההצלחה הטוב ביותר. אפשר גם להמשיך מהחדר האחרון או לבחור כל אחד מחמשת החדרים.

### 15.3 מבנה המערכת: כ-45 שניות

**מה להציג:** לעבור עם העכבר על החדרים ולהיכנס לחדר הראשון.

**מה לומר:**

> המערכת כתובה ב-Python ובנויה ב-Streamlit. הפרדתי בין הסביבות, האלגוריתמים, ה-Replay והממשק. הסוכן מתאמן מול מחלקות Python שמממשות reset ו-step. המשחק הגרפי מאפשר להבין ולשחק באותם חוקים, אבל הוא אינו מחליף את סביבת האימון. בכל חדר יש חמש לשוניות: משחק ידני, אימון, Replay, Analytics ומפרט מלא של הסביבה.

### 15.4 חדר 1: Dynamic Programming: כדקה

**מה להציג:** `Pac-Man Ice Maze`, תאים חלקלקים ורוחות. לפתוח גם את `Room Specs`.

**מה לומר:**

> החדר הראשון הוא Grid בגודל 10 על 10 והמודל שלו ידוע. המצב כולל את שורת ועמודת השחקן, פריטים שנאספו ואת שלב תנועת הרוחות. שלב הרוחות נמצא במצב כדי לשמור על תכונת מרקוב. קיימים תאים חלקלקים, ולכן הפעולה המבוקשת לא תמיד מתבצעת בדיוק. מכיוון שאני יודע את הסתברויות המעבר ואת התגמולים, השתמשתי ב-Value Iteration. האלגוריתם מחשב לכל מצב את תוחלת הערך של כל פעולה וחוזר על החישוב עד שהשינוי קטן מ-theta. לאחר ההתכנסות מופקת מדיניות שבוחרת את הפעולה בעלת הערך הגבוה ביותר.

### 15.5 חדר 2: SARSA: כדקה

**מה להציג:** `Sokoban Vault`, הארגז, המטרה והכספת.

**מה לומר:**

> בחדר השני המודל אינו ידוע לסוכן. השחקן צריך לדחוף את הארגז אל המטרה ורק אחר כך להגיע לכספת. גם כאן קיימים תאים חלקלקים. השתמשתי ב-SARSA, שהוא אלגוריתם on-policy. העדכון שלו משתמש בפעולה הבאה שהסוכן באמת בחר לפי מדיניות epsilon-greedy. לכן הוא לומד גם את הסיכון שנוצר מהחקירה עצמה, ולא רק את הערך של הפעולה החמדנית הטובה ביותר.

### 15.6 חדר 3: Q-Learning: כדקה

**מה להציג:** `Bomberman Reactor`, שני ה-COREs, הפצצות, המסיירים וה-WARP.

**מה לומר:**

> בחדר השלישי צריך לאסוף שני COREs, להימנע מפצצות וממסיירים, ולהגיע לשער. המצב כולל מסכת ביטים שמציינת אילו פריטים נאספו ואת שלב המסיירים. כאן השתמשתי ב-Q-Learning, שהוא off-policy. הפעולות בזמן האימון עדיין נבחרות עם epsilon-greedy, אבל יעד העדכון משתמש במקסימום ערכי Q במצב הבא. זה ההבדל המרכזי מ-SARSA, שמשתמש בפעולה הבאה שבאמת נבחרה.

### 15.7 חדר 4: Approximate Q-Learning: כדקה

**מה להציג:** `Lunar Lander Pad`, הנחתת, משטח הנחיתה והאסטרואידים.

**מה לומר:**

> החדר הרביעי אינו Grid. זהו חדר רציף בגודל 10 על 10 מטרים, והמצב הוא X, Y, Vx ו-Vy. בכל 0.02 שניות הסוכן בוחר מהירות, כאשר לכל רכיב מהירות יש ערך מינוס אחד, אפס או אחד. מכיוון שהמיקום רציף, אי אפשר לשמור טבלת Q לכל מצב. לכן השתמשתי ב-Approximate Q-Learning. ערך Q מחושב כסכום של תכונות כפול משקולות, והמשקולות מתעדכנות לפי שגיאת ה-TD. הוספתי גם reward shaping על התקדמות למטרה כדי לתת לסוכן אות למידה לפני ההצלחה הסופית.

### 15.8 חדר 5: תצפית חלקית והכללה: כדקה

**מה להציג:** `Portal Hazard Run`. לשנות את טווח התצפית או מספר המכשולים.

**מה לומר:**

> החדר החמישי הוא חדר הרשות והמורכב ביותר. קיימים בו פורטלים דינמיים ברוחב חצי מטר, שמיקומם וכיוון תנועתם נוצרים מחדש. הסוכן אינו רואה את כל החדר. הוא רואה רק את המכשול הקרוב ביותר שנמצא לפניו ובטווח התצפית שנבחר. פגיעה בפורטל גורמת לעונש, מעבר למיקום בטוח ואקראי ואיפוס המהירות. זה שונה מהרוחות בחדר הראשון, שרודפות ומחזירות להתחלה. אפשר לבדוק את המדיניות על seed חדש וכך לבחון הכללה לסביבה שלא הופיעה באימון.

### 15.9 אימון והייפר-פרמטרים: כדקה

**מה להציג:** לשונית `Train Agent` באחד מחדרי הלמידה.

**מה לומר:**

> במסך האימון אפשר לשלוט בכל הפרמטרים המרכזיים. Alpha קובע את עוצמת העדכון, gamma את חשיבות התגמולים העתידיים, ו-epsilon את הסתברות החקירה. epsilon decay מקטין את החקירה בהדרגה. Seed מאפשר לשחזר את אותו ניסוי ואת אותה אקראיות. הכפתור Optimize and train משווה כמה מועמדים באמצעות אימונים קצרים, נותן עדיפות להצלחה ולאחר מכן לתגמול ולמספר צעדים, ולבסוף מאמן את ההגדרה שקיבלה את הציון הגבוה ביותר.

### 15.10 Replay ו-Analytics: כדקה

**מה להציג:** לבחור אפיזודה מוקדמת ואפיזודה מאוחרת ב-`Episode Replay`, ואז לעבור ל-`Analytics`.

**מה לומר:**

> המערכת שומרת כל אפיזודה ולא רק את הטובות ביותר. ב-Replay אפשר לבחור כל ניסיון, לסנן הצלחות וכישלונות, לעצור ולהתקדם צעד אחר צעד. כך אפשר להשוות בין אפיזודה מוקדמת, שבה ההתנהגות כמעט אקראית, לבין אפיזודה מאוחרת שבה המדיניות כבר טובה יותר. ב-Analytics מוצגים התגמול, מספר הצעדים, שיעור ההצלחה, epsilon ושגיאת TD כאשר היא רלוונטית. כל ריצה נשמרת גם כ-CSV, JSON וגרף PNG.

### 15.11 סיכום: כ-30 שניות

**מה לומר:**

> לסיכום, הפרויקט מציג מעבר הדרגתי מתכנון עם מודל ידוע, דרך למידה טבלאית ללא מודל, ועד למידה עם פונקציות מקורבות ותצפית חלקית. בכל חדר התאמתי את הגדרת המצב, התגמולים והאלגוריתם למשימה. בנוסף, כל תהליך האימון ניתן למדידה, לשחזור ולצפייה מחדש, ולכן אפשר גם להסביר מה הסוכן למד ולא רק לראות אם הוא הצליח.

### 15.12 גרסה קצרה אם נשארות רק חמש דקות

1. פתיחה והצגת חמשת החדרים: 30 שניות.
2. חדר 1 והסבר Value Iteration: 45 שניות.
3. השוואת SARSA מול Q-Learning בחדרים 2 ו-3: דקה.
4. חדר 4 ופונקציות מקורבות: 45 שניות.
5. חדר 5, תצפית חלקית והכללה: 45 שניות.
6. Replay ו-Analytics: 45 שניות.
7. סיכום: 30 שניות.

## 16. משפט סיכום טוב להגנה

הפרויקט מדגים מעבר הדרגתי מתכנון עם מודל ידוע, דרך למידה טבלאית ללא מודל, ועד למידה עם פונקציות מקורבות ותצפית חלקית. בכל חדר הגדרת המצב, התגמול והאלגוריתם מותאמת לאופי המשימה, וכל תהליך הלמידה ניתן לשחזור, למדידה ולצפייה מחדש.
